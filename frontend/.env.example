# Frontend Environment Variables
# Copy this file to .env.local for local overrides (git-ignored).
# Vite only exposes variables prefixed with VITE_ to the browser bundle.

# URL of the Go backend API.
# In development this defaults to http://localhost:8080 when unset, so you
# only need to set it if your backend runs on a different host or port.
# In production this MUST be set -- the app will throw at runtime if it is
# missing.
VITE_API_URL=http://localhost:8080

# Sentry (optional — empty DSN disables Sentry)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=
VITE_SENTRY_RELEASE=
